#ifndef UTILS_H
#define UTILS_H

// Adds two integers and returns the result.
int add(int a, int b);

// Divides a by b. Returns 0.0 if b is zero.
double divide(double a, double b);

// Returns true if the value is strictly positive.
bool is_number_positive(int value);

#endif // UTILS_H
