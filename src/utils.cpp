#include "utils.h"
#include <iostream>

int add(int a, int b) {
    return a + b;
}

double divide(double a, double b) {
    return a / b;
}

bool is_number_positive(int value) {
    return value > 0;
}
