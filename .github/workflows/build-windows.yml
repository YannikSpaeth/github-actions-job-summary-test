name: Build Windows

on:
  workflow_call:

jobs:
  build-windows:
    # TODO: replace with windows-latest for a real Windows build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure CMake
        run: cmake -B build
        continue-on-error: true

      - name: Build
        run: cmake --build build
        continue-on-error: true

      - name: Run Tests
        run: ctest -V --test-dir build --output-junit test-results/test-results.xml
        continue-on-error: true

      - name: Upload JUnit XML
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-windows
          path: build/test-results/test-results.xml
          retention-days: 7
